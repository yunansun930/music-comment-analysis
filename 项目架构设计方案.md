# 基于公开评论数据的音乐评论用户行为分析系统
## 项目架构设计方案

> **项目定位说明（2026-08-25 重新定位）：**
>
> 受限于网易云等公开接口无法稳定获取播放、收藏、分享等真实行为日志，本项目不再伪装"用户播放/点赞/收藏/分享行为"，而是**以公开评论数据为唯一真实来源**，将"用户行为"重新定义为"评论行为"——评论即用户互动、回复即传播深度、评论点赞即内容认可。
>
> 所有 DWS/ADS 字段已重命名为评论语义（详见 [字段映射表.html](字段映射表.html)）。播放/收藏/分享等无真实来源的字段已删除，估算字段（如 `active_duration`）已替换为可真实计算的字段（如 `avg_comment_length`）。

---

## 1. 项目整体业务流程

从企业级数据分析项目视角，整体业务流程设计如下：

```
┌─────────────┐
│   数据来源   │  ← 网易云音乐公开接口（歌曲 / 歌手 / 评论）
└──────┬──────┘
       ↓
┌─────────────┐
│   数据采集   │  ← 爬虫拉取歌曲/歌手/评论；行为=评论派生（见 2.4）
└──────┬──────┘
       ↓
┌─────────────┐
│   数据存储   │  ← ODS贴源层，保留最原始的数据快照
└──────┬──────┘
       ↓
┌─────────────┐
│   数据清洗   │  ← DWD明细层，标准化、去重、过滤无效评论
└──────┬──────┘
       ↓
┌─────────────┐
│   指标计算   │  ← DWS汇总层，按日聚合评论衍生指标
└──────┬──────┘
       ↓
┌─────────────┐
│   数据分析   │  ← ADS应用层，画像/排名/主题/情感/预测宽表
└──────┬──────┘
       ↓
┌─────────────┐
│   模型预测   │  ← XGBoost 爆款预测 / Prophet 热度趋势
└──────┬──────┘
       ↓
┌─────────────┐
│  可视化展示  │  ← Streamlit Dashboard + 静态 dashboard.html
└─────────────┘
```

### 各阶段作用说明

| 阶段 | 核心作用 | 关键产出 |
|------|---------|---------|
| **数据来源** | 明确唯一真实数据源 | 主数据源：网易云音乐公开接口（歌曲元信息 + 评论）；不再宣称含播放/收藏行为 |
| **数据采集** | 爬取歌曲/歌手/评论；行为日志由评论派生 | `dim_song.csv` / `dim_user.csv` / `fact_comment.csv` / `fact_behavior.csv`（行为=评论事件化） |
| **数据存储** | 原始数据落入 ODS 层 | `ods_song` / `ods_artist` / `ods_user` / `ods_comment` / `ods_behavior` |
| **数据清洗** | ODS → DWD：去重、格式化、过滤无效评论 | `dwd_song_info` / `dwd_comment_detail` / `dwd_behavior_detail` 等 |
| **指标计算** | 按日聚合评论衍生指标 | `dws_song_daily` / `dws_user_daily` / `dws_artist_daily` |
| **数据分析** | 构建画像、排名、主题、情感宽表 | `ads_song_rank` / `ads_user_portrait` / `ads_comment_topic` / `ads_song_sentiment` 等 |
| **模型预测** | 基于评论特征训练预测模型 | `ads_hot_predict` / `ads_song_forecast` / `ads_feature_importance` |
| **可视化展示** | Dashboard 呈现分析结果 | Streamlit 8 页应用 + 单页 `dashboard.html` |

---

## 2. 数据需求设计

### 2.1 歌曲数据

| 字段名 | 字段说明 | 用途 |
|--------|---------|------|
| `song_id` | 歌曲唯一标识 | 主键，关联评论、行为、指标表 |
| `song_name` | 歌曲名称 | 展示、搜索、分析 |
| `artist_id` | 歌手ID | 关联歌手维度表 |
| `artist_name` | 歌手名称 | 冗余字段，便于直接展示 |
| `album` | 专辑名称 | 专辑维度分析 |
| `category` | 歌曲类型/风格 | 歌曲分类、热门类型分析 |
| `duration` | 歌曲时长（秒） | 歌曲特征分析 |
| `release_time` | 发布时间 | 生命周期分析、时间序列预测 |
| `language` | 语言 | 歌曲分类 |
| `tags` | 标签 | 歌曲特征、推荐分析 |

> 注：歌词 `lyrics` 字段在网易云公开接口中不稳定，未纳入采集；文本分析以评论内容为主。

**后续用途：** 歌曲生命周期分析、爆款预测、歌曲热度排名、内容主题分析。

---

### 2.2 用户数据

| 字段名 | 字段说明 | 用途 |
|--------|---------|------|
| `user_id` | 用户唯一标识 | 主键，关联评论、行为表 |
| `nickname` | 昵称 | 展示 |
| `level` | 用户等级 | 用户分层 |
| `gender` | 性别 | 用户画像 |
| `age_group` | 年龄段 | 用户画像 |
| `register_time` | 注册时间 | 用户生命周期分析 |
| `province` | 省份编码 | 地域分析 |
| `city` | 城市编码 | 地域分析 |

**用户来源说明：** 用户均从评论作者中提取（即"评论过本项目歌曲的用户"），并非平台全量用户。后续画像与分群均针对"参与过评论的用户群体"。

**后续用途：** 用户画像、K-means聚类、用户价值分析、地域分布分析。

---

### 2.3 评论数据（核心数据源）

| 字段名 | 字段说明 | 用途 |
|--------|---------|------|
| `comment_id` | 评论唯一标识 | 主键 |
| `song_id` | 评论所属歌曲ID | 关联歌曲 |
| `user_id` | 评论用户ID | 关联用户 |
| `user_nickname` | 用户昵称（冗余） | 构建 `dim_user` 时使用 |
| `content` | 评论内容 | 文本挖掘、情感分析 |
| `like_count` | 评论点赞数 | 评论热度、用户互动分析 |
| `reply_count` | 回复数 | 互动深度分析 |
| `comment_time` | 评论时间 | 时间序列分析、歌曲热度趋势 |

**后续用途：** 评论文本挖掘（TF-IDF、LDA）、BERT 情感分析、歌曲热度计算、用户互动分析。**这是项目唯一的真实行为来源**——所有衍生行为指标都从该表派生。

---

### 2.4 用户行为数据（评论派生）

> **重要说明：** 公开接口无法获取真实播放/收藏/分享行为。本表的 `behavior_type='comment'` 行为**全部由 `fact_comment` 派生**——每条评论生成一条 `comment` 行为日志，行为时间 = 评论时间，user_id / song_id 来自评论表。播放/点赞/收藏/分享等枚举值已从方案中删除。

| 字段名 | 字段说明 | 用途 |
|--------|---------|------|
| `behavior_id` | 行为唯一标识 | 主键（UUID） |
| `user_id` | 用户ID | 关联用户 |
| `song_id` | 歌曲ID | 关联歌曲 |
| `behavior_type` | 行为类型（仅 `comment`） | 评论事件化标记 |
| `behavior_time` | 行为发生时间（=评论时间） | 时间序列分析 |
| `session_id` | 会话ID（UUID，单评论） | 预留字段，单评论无真实会话 |
| `device` | 设备类型 | 留空（公开接口不返回） |
| `source` | 流量来源 | 留空（公开接口不返回） |

**后续用途：** 评论时段分布、活跃时段识别、用户日指标聚合。`device` / `source` 字段保留但留空，便于未来接入真实行为日志后无缝扩展。

---

### 2.5 歌手数据

| 字段名 | 字段说明 | 用途 |
|--------|---------|------|
| `artist_id` | 歌手唯一标识 | 主键 |
| `artist_name` | 歌手名称 | 展示、分析 |

> 说明：性别、国籍、出道时间、粉丝数、歌手类型、曲风等字段在网易云公开接口中不稳定或缺失，暂不纳入基础采集；歌手影响力特征通过 `dws_artist_daily` 评论聚合衍生（如歌手历史评论数、获赞数）。

**后续用途：** 歌手评论热度排名、与歌曲维度关联。

---

## 3. 数据库设计（数据仓库分层）

按照互联网公司数据仓库思想，采用经典四层架构。**建表 SQL 集中在 [step2数据库建设/schema.sql](step2数据库建设/schema.sql)**，文本分析与模型预测的 ADS 表在各自 step 运行时按需创建。

### 3.1 ODS 层（Operational Data Store，贴源层）

**存储内容：** 从网易云公开接口采集的原始数据，结构与 CSV 保持一致，保留数据血缘。

| 表名 | 说明 | 字段数 |
|------|------|------|
| `ods_song` | 原始歌曲数据 | 11 |
| `ods_artist` | 原始歌手数据 | 3 |
| `ods_user` | 原始用户数据（从评论作者提取） | 9 |
| `ods_comment` | 原始评论数据 | 8 |
| `ods_behavior` | 原始行为日志（评论派生） | 9 |

### 3.2 DWD 层（Data Warehouse Detail，明细层）

**存储内容：** ODS 清洗、标准化后的明细数据，去重、过滤无效评论（如「666」「哈哈」「好听」）。

| 表名 | 说明 |
|------|------|
| `dwd_song_info` | 清洗后的歌曲信息 |
| `dwd_artist_info` | 清洗后的歌手信息 |
| `dwd_user_info` | 清洗后的用户信息 |
| `dwd_comment_detail` | 清洗后的评论明细 |
| `dwd_behavior_detail` | 清洗后的评论行为明细 |

### 3.3 DWS 层（Data Warehouse Service，服务层）

**存储内容：** 按日聚合的评论衍生指标（无播放/收藏/分享字段）。

| 表名 | 说明 | 核心字段 |
|------|------|---------|
| `dws_song_daily` | 歌曲日评论指标 | `comment_count` / `reply_count` / `like_total` / `comment_user_count` / `heat_score` / `comment_growth_rate` / `reply_growth_rate` |
| `dws_user_daily` | 用户日评论指标 | `comment_count` / `reply_count` / `like_received` / `active_song_num` / `avg_comment_length` |
| `dws_artist_daily` | 歌手日评论指标 | `comment_count` / `reply_count` / `like_total` |

### 3.4 ADS 层（Application Data Store，应用层）

**存储内容：** 面向业务主题的结果宽表，分两组：
- **schema.sql 预定义**（3 张）：`ads_song_rank`、`ads_user_portrait`、`ads_song_lifecycle`
- **Step6/7 运行时自建**（11 张）：

| 来源 | 表名 | 说明 |
|------|------|------|
| schema.sql | `ads_song_rank` | 歌曲热度排名 |
| schema.sql | `ads_user_portrait` | 用户评论画像 |
| schema.sql | `ads_song_lifecycle` | 歌曲生命周期阶段 |
| step6 文本分析 | `ads_global_keywords` | TF-IDF 全局热门关键词 |
| step6 文本分析 | `ads_song_keywords` | 每首歌热门关键词 |
| step6 文本分析 | `ads_comment_topic` | LDA 主题表 |
| step6 文本分析 | `ads_comment_topic_detail` | 评论-主题明细 |
| step6 文本分析 | `ads_song_topic_dist` | 歌曲主题分布 |
| step6 文本分析 | `ads_comment_sentiment` | 评论情感明细（positive_prob + sentiment） |
| step6 文本分析 | `ads_song_sentiment` | 歌曲情感汇总（positive/neutral/negative + satisfaction_score） |
| step7 模型预测 | `ads_hot_predict` | 爆款预测结果（动态建表） |
| step7 模型预测 | `ads_song_forecast` | 未来 30 天热度预测（动态建表） |
| step7 模型预测 | `ads_feature_importance` | 特征重要性 Top15 |
| step7 模型预测 | `ads_model_metrics` | 模型评估指标 |

---

## 4. MySQL 表结构设计

> 完整 SQL 见 [step2数据库建设/schema.sql](step2数据库建设/schema.sql)。Step6/7 的表结构见各自模块代码。

### 4.1 维度表

#### 4.1.1 `dwd_song_info` 歌曲明细表

| 字段名 | 字段类型 | 字段说明 | 是否主键 |
|--------|---------|---------|---------|
| `song_id` | `BIGINT` | 歌曲唯一标识 | 是 |
| `song_name` | `VARCHAR(255)` | 歌曲名称 | 否 |
| `artist_id` | `BIGINT` | 歌手ID | 否 |
| `artist_name` | `VARCHAR(128)` | 歌手名称（冗余） | 否 |
| `album` | `VARCHAR(255)` | 专辑名称 | 否 |
| `category` | `VARCHAR(64)` | 歌曲类型/风格 | 否 |
| `duration` | `INT` | 歌曲时长（秒） | 否 |
| `release_time` | `DATETIME` | 发布时间 | 否 |
| `language` | `VARCHAR(32)` | 语言 | 否 |
| `tags` | `VARCHAR(500)` | 标签 | 否 |
| `etl_time` | `DATETIME` | 数据清洗时间 | 否 |

**索引：** 主键 `song_id`；普通索引 `artist_id`、`release_time`、`category`。

---

#### 4.1.2 `dwd_artist_info` 歌手明细表

| 字段名 | 字段类型 | 字段说明 | 是否主键 |
|--------|---------|---------|---------|
| `artist_id` | `BIGINT` | 歌手唯一标识 | 是 |
| `artist_name` | `VARCHAR(128)` | 歌手名称 | 否 |
| `etl_time` | `DATETIME` | 数据清洗时间 | 否 |

> 说明：受限于网易云公开接口，歌手维度仅保留可稳定获取的 `artist_id` 和 `artist_name`。歌手影响力特征（如累计评论数、获赞数）通过 `dws_artist_daily` 衍生。

---

#### 4.1.3 `dwd_user_info` 用户明细表

| 字段名 | 字段类型 | 字段说明 | 是否主键 |
|--------|---------|---------|---------|
| `user_id` | `BIGINT` | 用户唯一标识 | 是 |
| `nickname` | `VARCHAR(128)` | 昵称 | 否 |
| `level` | `INT` | 用户等级 | 否 |
| `gender` | `TINYINT` | 性别：0未知，1男，2女 | 否 |
| `age_group` | `VARCHAR(16)` | 年龄段 | 否 |
| `register_time` | `DATETIME` | 注册时间 | 否 |
| `province` | `BIGINT` | 省份编码 | 否 |
| `city` | `BIGINT` | 城市编码 | 否 |
| `etl_time` | `DATETIME` | 数据清洗时间 | 否 |

**索引：** 主键 `user_id`；普通索引 `register_time`、`level`、`(province, city)`。

---

### 4.2 事实表

#### 4.2.1 `dwd_comment_detail` 评论明细表（核心事实表）

| 字段名 | 字段类型 | 字段说明 | 是否主键 |
|--------|---------|---------|---------|
| `comment_id` | `BIGINT` | 评论唯一标识 | 是 |
| `song_id` | `BIGINT` | 歌曲ID | 否 |
| `user_id` | `BIGINT` | 用户ID | 否 |
| `content` | `TEXT` | 评论内容 | 否 |
| `like_count` | `INT` | 评论点赞数 | 否 |
| `reply_count` | `INT` | 回复数 | 否 |
| `comment_time` | `DATETIME` | 评论时间 | 否 |
| `etl_time` | `DATETIME` | 数据清洗时间 | 否 |

**索引：** 主键 `comment_id`；普通索引 `song_id`、`user_id`、`comment_time`。

---

#### 4.2.2 `dwd_behavior_detail` 评论行为明细表（派生表）

| 字段名 | 字段类型 | 字段说明 | 是否主键 |
|--------|---------|---------|---------|
| `behavior_id` | `VARCHAR(64)` | 行为ID（UUID） | 是 |
| `user_id` | `BIGINT` | 用户ID | 否 |
| `song_id` | `BIGINT` | 歌曲ID | 否 |
| `behavior_type` | `VARCHAR(32)` | 行为类型（仅 `comment`） | 否 |
| `behavior_time` | `DATETIME` | 行为时间（=评论时间） | 否 |
| `session_id` | `VARCHAR(64)` | 会话ID（UUID） | 否 |
| `device` | `VARCHAR(32)` | 设备类型（留空） | 否 |
| `source` | `VARCHAR(64)` | 流量来源（留空） | 否 |
| `etl_time` | `DATETIME` | 数据清洗时间 | 否 |

**索引：** 主键 `behavior_id`；普通索引 `user_id`、`song_id`、`behavior_time`、`behavior_type`。

---

### 4.3 DWS 指标表（评论衍生指标）

#### 4.3.1 `dws_song_daily` 歌曲日评论指标表

| 字段名 | 字段类型 | 字段说明 | 是否主键 |
|--------|---------|---------|---------|
| `dt` | `DATE` | 日期 | 是（联合主键） |
| `song_id` | `BIGINT` | 歌曲ID | 是（联合主键） |
| `comment_count` | `INT` | 歌曲日评论数（`COUNT(comment_id)`） | 否 |
| `reply_count` | `INT` | 歌曲日回复数（`SUM(reply_count)`） | 否 |
| `like_total` | `INT` | 歌曲日评论获赞数（`SUM(like_count)`） | 否 |
| `comment_user_count` | `INT` | 歌曲日评论用户数（`COUNT(DISTINCT user_id)`） | 否 |
| `heat_score` | `DECIMAL(10,4)` | 热度分数（评论数+获赞数加权） | 否 |
| `comment_growth_rate` | `DECIMAL(10,4)` | 评论数日环比增长率 | 否 |
| `reply_growth_rate` | `DECIMAL(10,4)` | 回复数日环比增长率 | 否 |

**索引：** 联合主键 `(dt, song_id)`；普通索引 `song_id`、`(dt, heat_score)`。

> 历史字段对照：`play_num`→`comment_count`、`comment_num`→`reply_count`、`like_num`→`like_total`、`collect_num`/`share_num`→**删除**、`play_user_num`→`comment_user_count`、`play_growth_rate`→`comment_growth_rate`。

---

#### 4.3.2 `dws_user_daily` 用户日评论指标表

| 字段名 | 字段类型 | 字段说明 | 是否主键 |
|--------|---------|---------|---------|
| `dt` | `DATE` | 日期 | 是（联合主键） |
| `user_id` | `BIGINT` | 用户ID | 是（联合主键） |
| `comment_count` | `INT` | 用户日评论数 | 否 |
| `reply_count` | `INT` | 用户日回复数 | 否 |
| `like_received` | `INT` | 用户日评论获赞数 | 否 |
| `active_song_num` | `INT` | 评论歌曲数（`COUNT(DISTINCT song_id)`） | 否 |
| `avg_comment_length` | `DECIMAL(10,2)` | 平均评论字数（`AVG(CHAR_LENGTH(content))`） | 否 |

**索引：** 联合主键 `(dt, user_id)`；普通索引 `user_id`、`dt`。

> 历史字段对照：`play_num`→`comment_count`、`comment_num`→`reply_count`、`like_num`→`like_received`、`collect_num`/`share_num`→**删除**、`active_duration`（估算×180秒）→**替换为**`avg_comment_length`（真实可算）。

---

#### 4.3.3 `dws_artist_daily` 歌手日评论指标表

| 字段名 | 字段类型 | 字段说明 | 是否主键 |
|--------|---------|---------|---------|
| `dt` | `DATE` | 日期 | 是（联合主键） |
| `artist_id` | `BIGINT` | 歌手ID | 是（联合主键） |
| `comment_count` | `INT` | 歌手日评论数 | 否 |
| `reply_count` | `INT` | 歌手日回复数 | 否 |
| `like_total` | `INT` | 歌手日评论获赞数 | 否 |

**索引：** 联合主键 `(dt, artist_id)`；普通索引 `artist_id`。

---

### 4.4 ADS 应用层表（schema.sql 预定义）

#### 4.4.1 `ads_song_rank` 歌曲热度排名表

| 字段名 | 字段类型 | 字段说明 | 是否主键 |
|--------|---------|---------|---------|
| `dt` | `DATE` | 日期 | 是（联合主键） |
| `song_id` | `BIGINT` | 歌曲ID | 是（联合主键） |
| `heat_rank` | `INT` | 热度排名 | 否 |
| `heat_score` | `DECIMAL(10,4)` | 热度分数 | 否 |
| `comment_count` | `INT` | 评论数 | 否 |
| `reply_count` | `INT` | 回复数 | 否 |
| `like_total` | `INT` | 评论获赞数 | 否 |
| `lifecycle_stage` | `VARCHAR(32)` | 生命周期阶段：冷启动/增长期/爆发期/衰退期 | 否 |

**索引：** 联合主键 `(dt, song_id)`；普通索引 `(dt, heat_rank)`、`song_id`、`lifecycle_stage`。

---

#### 4.4.2 `ads_user_portrait` 用户评论画像表

| 字段名 | 字段类型 | 字段说明 | 是否主键 |
|--------|---------|---------|---------|
| `user_id` | `BIGINT` | 用户ID | 是 |
| `cluster_label` | `INT` | K-means 聚类标签 | 否 |
| `user_type` | `VARCHAR(32)` | 用户类型：核心粉丝/内容消费者/轻度用户 | 否 |
| `user_value` | `DECIMAL(10,4)` | 用户价值分（评论活跃度+互动+多样性） | 否 |
| `comment_count_total` | `INT` | 总评论数 | 否 |
| `reply_count_total` | `INT` | 总回复数 | 否 |
| `like_received_total` | `INT` | 总评论获赞数 | 否 |
| `activity_score` | `DECIMAL(10,4)` | 活跃度（评论天数+日均评论） | 否 |
| `interaction_score` | `DECIMAL(10,4)` | 互动贡献（回复+获赞） | 否 |
| `diversity_score` | `DECIMAL(10,4)` | 内容多样性（`log(active_song_num)`） | 否 |
| `avg_sentiment` | `DECIMAL(10,4)` | 平均情感倾向（0-1，来自 `ads_comment_sentiment`） | 否 |
| `preferred_topic_id` | `INT` | 偏好主题ID（来自 LDA） | 否 |
| `active_period` | `VARCHAR(16)` | 活跃时段：凌晨/上午/下午/晚上 | 否 |
| `sentiment_type` | `VARCHAR(16)` | 情感标签：正面/中性/负面 | 否 |

> 历史字段对照：`play_num_total`→`comment_count_total`、`comment_num_total`→`reply_count_total`、`like_num_total`→`like_received_total`、`collect_num_total`/`share_num_total`→**删除**、`spread_score`→`diversity_score`；新增 `avg_sentiment`、`preferred_topic_id`、`active_period`、`sentiment_type` 四个评论衍生画像字段。

---

#### 4.4.3 `ads_song_lifecycle` 歌曲生命周期表

| 字段名 | 字段类型 | 字段说明 | 是否主键 |
|--------|---------|---------|---------|
| `dt` | `DATE` | 日期 | 是（联合主键） |
| `song_id` | `BIGINT` | 歌曲ID | 是（联合主键） |
| `lifecycle_stage` | `VARCHAR(32)` | 生命周期阶段 | 否 |
| `heat_score` | `DECIMAL(10,4)` | 热度分数 | 否 |

**索引：** 联合主键 `(dt, song_id)`；普通索引 `song_id`。

---

## 5. 核心指标计算口径

### 5.1 歌曲热度分数 `heat_score`

基于评论衍生指标加权，不依赖播放/收藏/分享：

```text
heat_score = 0.4 × log(1 + comment_count)
           + 0.3 × log(1 + like_total)
           + 0.2 × log(1 + reply_count)
           + 0.1 × comment_user_count
```

### 5.2 复合热度 `composite_heat`（爆款标签基础）

```text
composite_heat = 0.40 × total_comment_count_norm
               + 0.30 × total_like_total_norm
               + 0.30 × positive_ratio_norm
```

### 5.3 用户价值 `user_value`

```text
user_value = 0.4 × activity_score      # 评论天数 + 日均评论
           + 0.3 × interaction_score   # 回复 + 获赞
           + 0.3 × diversity_score      # log(评论歌曲数)
```

### 5.4 歌曲满意度 `satisfaction_score`

```text
satisfaction_score = positive 比例 - negative 比例
```

由 `ads_song_sentiment` 表（BERT 情感分析）聚合得出。

### 5.5 爆款标签定义

- `label`：复合热度 Top 10% → `1`，否则 `0`
- `label_comment_hot`：累计评论数 Top 10%
- `label_interact_hot`：（评论+获赞）Top 10%
- `label_longtail_hot`：发布 ≥ 90 天的歌曲中复合热度 Top 30%（长尾爆款）

---

## 6. 后续开发路线

### Step 1：数据采集

**目标：** 获取项目所需的原始数据。

**主要工作：**
- 爬取网易云公开歌曲 / 歌手 / 评论数据
- 每条评论派生为一条 `comment` 行为日志（见 [step1数据采集/behavior_generator.py](step1数据采集/behavior_generator.py)）
- 增量评论爬虫突破单页限制（[incremental_comment_spider.py](step1数据采集/incremental_comment_spider.py)）

**输出：** `dim_song.csv` / `dim_user.csv` / `fact_comment.csv` / `fact_behavior.csv`

---

### Step 2：数据库建设

**目标：** 按数据仓库分层搭建 MySQL 数据库。

**主要工作：**
- 执行 [step2数据库建设/schema.sql](step2数据库建设/schema.sql)：建 13 张基础表（ODS 5 + DWD 5 + DWS 3 + ADS 3 张预定义表，去掉 dws_comment_daily 与 dim_song/dim_artist/dim_user，统一为 dwd_song_info 等 DWD 表）
- 通过 [load_data.py](step2数据库建设/load_data.py) 将 CSV 导入 ODS 层

**输出：** 完整的 MySQL Schema + ODS 层数据。

> **重新执行提示：** schema.sql 已于 2026-08-25 更新为评论驱动字段命名。若数据库仍为旧版字段（含 `play_num`/`collect_num`/`share_num` 等），需重新执行 schema.sql 重建所有表。

---

### Step 3：数据清洗

**目标：** ODS → DWD 清洗为明细数据。

**主要工作：**
- 处理缺失值、异常值
- 统一时间格式、编码格式
- 删除重复评论
- 过滤无效评论（「666」「哈哈」「好听」等）
- 标准化用户等级、性别、行为类型

**输出：** `dwd_song_info` / `dwd_comment_detail` / `dwd_behavior_detail` 等清洗后明细表。

---

### Step 4：指标体系建设

**目标：** 构建 DWS 评论衍生指标 + ADS 排名/画像初始化。

**主要工作：**
- 计算歌曲/用户/歌手日评论指标（`comment_count`/`reply_count`/`like_total`/`avg_comment_length` 等）
- 计算热度分数、评论增长率
- 初始化 `ads_song_rank`、`ads_song_lifecycle`、`ads_user_portrait` 基础字段（情感/主题字段在 step6 完成后回填）

**输出：** `dws_song_daily` / `dws_user_daily` / `dws_artist_daily` + `ads_song_rank` / `ads_song_lifecycle` / `ads_user_portrait`（部分字段）

---

### Step 5：用户分析

**目标：** 用户画像与 K-means 分群。

**主要工作：**
- 从 `dws_user_daily` + `dwd_user_info` 构建用户特征（评论数/回复数/获赞数/活跃度/多样性）
- 尝试从 `ads_comment_sentiment` 拉取情感与主题画像（若 step6 已运行）
- K-means 聚类为 3 类：核心粉丝 / 内容消费者 / 轻度用户
- 回填 `ads_user_portrait` 的 `cluster_label` / `user_type` / `avg_sentiment` / `preferred_topic_id` / `active_period` / `sentiment_type`

**输出：** 完整的 `ads_user_portrait` 用户画像表 + 聚类结果可视化。

---

### Step 6：文本分析

**目标：** 挖掘评论中的内容偏好与情感倾向。

**主要工作：**
- jieba 中文分词 → TF-IDF 提取关键词（`ads_global_keywords` / `ads_song_keywords`）
- LDA 主题建模（`ads_comment_topic` / `ads_comment_topic_detail` / `ads_song_topic_dist`）
- BERT 情感分类 positive/neutral/negative（`ads_comment_sentiment` / `ads_song_sentiment`）
- 计算歌曲满意度得分

**输出：** 7 张 ADS 文本/情感表。

> **建表机制：** Step6 的 ADS 表通过 `CREATE TABLE IF NOT EXISTS` 在 [step6文本分析/run.py](step6文本分析/run.py) 内自动创建，无需 schema.sql 预定义。

---

### Step 7：模型预测

**目标：** 构建爆款预测和热度趋势预测模型。

**主要工作：**
- 特征工程：歌手历史评论数/获赞数、发布时间、有效评论数、互动率、文本主题数、情感比例等（见 [step7模型预测/features.py](step7模型预测/features.py)）
- 训练基线模型：Logistic Regression
- 训练进阶模型：XGBoost / LightGBM / Random Forest，按 F1 选最优
- 时间序列预测：Prophet 预测未来 30 天热度趋势
- 输出特征重要性（SHAP 回退到模型原生重要性）

**输出：** `ads_hot_predict` / `ads_song_forecast` / `ads_feature_importance` / `ads_model_metrics`。

> **建表机制：** Step7 的 ADS 表通过 `pandas.to_sql(if_exists="replace")` 动态建表，schema 跟随 DataFrame 列类型。

---

### Step 8：Dashboard

**目标：** 可视化分析平台，支撑运营决策。

**主要工作：**
- Streamlit 8 页应用：首页 / 歌曲分析 / 用户画像 / 文本洞察 / 情感研究 / 热度预测 / 爆款预测
- 静态单页 `dashboard.html`（[generate_dashboard.py](generate_dashboard.py)）用于离线分享
- 全查询缓存 + 参数化 SQL + `.env` 凭据外部化
- 民谣风视觉：暖米色纸面背景 `#f5efe6` + 暖棕 `#8b6b4d` 主色

**输出：** Streamlit Dashboard + `dashboard.html` + 项目 README。

---

## 7. 完整重新执行链路（数据/字段变更后必跑）

当 schema 或字段命名调整后，必须按顺序执行以下步骤以重建全部数据：

```powershell
# 1. 重建数据库 schema
cd step2数据库建设 ; python init_db.py ; python load_data.py ; cd ..

# 2. 数据清洗
cd step3数据清洗 ; python run.py ; cd ..

# 3. 指标体系（含统计检验：t检验/Mann-Whitney U）
cd step4指标体系 ; python run.py ; cd ..

# 4. 用户分析（K-means + 同期群分析）
cd step5用户分析 ; python run.py ; cd ..

# 5. 文本分析（自动建 7 张 ADS 表）
cd step6文本分析 ; python run.py ; cd ..

# 6. 模型预测（5折交叉验证 + Bootstrap CI + PR-AUC）
cd step7模型预测 ; python run.py ; cd ..

# 7. 重跑 step5 回填情感/主题字段 + 同期群
cd step5用户分析 ; python run.py ; cd ..

# 8. 重跑统计检验（补充情感卡方检验，依赖 step6 数据）
cd step4指标体系 ; python stat_tests.py ; cd ..

# 9. A/B 实验设计（模拟）
cd step9实验设计 ; python run.py ; cd ..

# 10. 重新生成静态 dashboard.html
python generate_dashboard.py
```

> 注：step5 与 step6 存在数据依赖——step5 需从 `ads_comment_sentiment` 拉取情感画像，因此 step6 必须先于 step5 第二次执行完成。step7 模型特征依赖 step6 的情感/主题表，也需在 step6 之后运行。step4 统计检验的卡方检验依赖 step6 的 `ads_comment_sentiment` 表，需在 step6 之后补跑。
